kind = "web"
previewPath = "/"
title = "Burger Nomr"
version = "1.0.0"
id = "artifacts/burger-nomr"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20840

[services.development]
run = "pnpm --filter @workspace/burger-nomr run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/burger-nomr", "run", "build" ]
publicDir = "artifacts/burger-nomr/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20840"
BASE_PATH = "/"
